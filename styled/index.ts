import styled, { CreateStyled } from '@emotion/styled'
import { ThemeType } from './theme'
import { mqMax, mqMin } from './breakpoints'

export { mqMax, mqMin }

export default styled as CreateStyled<ThemeType>
